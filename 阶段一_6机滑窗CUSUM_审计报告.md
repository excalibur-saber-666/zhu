# 阶段一 6机滑动窗口 CUSUM 审计报告

## 审计范围

对象为 2 架僚机、4 架高精度长机的独立六机实验。原始五机基线未被替换。
本报告使用 `seeds = [21, 22, 23]`、完整 600 s 轨迹、同一缓存噪声和在线 CUSUM 决策。

## 已确认并修复的问题

### 1. 滑窗测距因子曾被重复白化

原始 `residual_cal` 已固定按 `sigma_dis = 0.2 m` 白化。旧滑窗路径又使用
`sqrt(weight) / range_std`，等效把测距因子额外放大 5 倍、信息量额外放大 25 倍。
这会使旧版本的滑窗结果不适合与单历元图公平比较。

现已改为 `range_residual_cal` 产生米单位残差和雅可比，再在滑窗内一次性应用
`sqrt(weight) / range_std`。单历元路径在固定 `sigma_dis = 0.2 m` 的 Stage-1 配置下与其一致。

### 2. 标定完成后 `baseline_unverified` 未清除

该字段只影响诊断，不影响权重；现已在成功完成边标定后置为 `false`，并由单元测试覆盖。

## 坐标、链路与几何

`posical_enu` 的输入是相对原点的局部 `[E; N; U]` 米坐标，输出才是经纬高。因此
`[600; 250; 300]` 并未被误当成经纬高。正反换算的数值误差为约 `0.000114 m`。

Leader4 在完整 600 s 内的检查结果为：

| 僚机 | 与 Leader4 距离范围 | `(follower,6)` 在线率 | 4 长机最小奇异值 | 最大条件数 |
| --- | ---: | ---: | ---: | ---: |
| Follower1 | 400.273–400.279 m | 100% | 0.501601 | 2.918 |
| Follower2 | 226.385–226.387 m | 100% | 0.189588 | 8.204 |

任意留一架长机后，剩余三条长机视线仍全程为 3D 满秩。最差情况为 Follower2 去掉
Leader3：最小奇异值 `0.062272`、最大条件数 `24.959`。因此新增 Leader4 提供了有效几何冗余，
不只是重复边。

## 图模型性质

- 当前滑窗是**固定长度的移动窗口批量优化**（moving-window batch optimization）。
- 最老帧及其因子直接移出；没有 Schur 补边缘化，因此不应称为严格的 marginalized fixed-lag smoother。
- 运动约束是“图更新后到下一图更新前”的 SINS 位置增量，采用经验标准差 `[2;2;4] m`。
  它不是严格独立的 IMU 预积分因子，可能与 KF/图反馈存在相关性。
- 已提供 `cfg.sliding_window_motion_enable` 以便消融。关闭它时，多帧等权滑窗与单历元
  Equal-FGO 在 `1e-7 m` 数值容差内一致，说明跨帧影响仅由显式运动因子传播。

## 必要回归

- `test_stage1_window_length_one_regression`：窗口长度 1 的等权滑窗退化为单历元 Equal-FGO，已通过。
- `test_stage1_sliding_without_motion_regression`：长度 10、但无运动因子的滑窗退化为独立单历元，已通过。
- `test_factor_graph_sliding_window`：测距因子只白化一次，已通过。
- `test_stage1_redundant_geometry`：坐标、链路、全量及留一几何，已通过。

## 3 m 主故障的多种子消融结果

故障为 `(2,3)`、100–150 s、`+3 m`。下表为故障窗口 3D RMSE 的三种子平均值。

| 方法 | Follower1 | Follower2 |
| --- | ---: | ---: |
| 单历元 Equal-FGO | 1.646437 | 1.613595 |
| 单历元 CUSUM-FGO | 0.897276 | 0.937144 |
| 滑窗 Equal-FGO | 1.637132 | 1.610104 |
| 滑窗 CUSUM，仅软降权 | 0.880653 | 0.915212 |
| 滑窗 CUSUM，软降权 + 在线选择性隔离 | 0.792812 | 0.960210 |

结论：CUSUM 是主要贡献；等权滑窗的影响约为 0–1%。选择性隔离对 Follower1 有额外改善，
但 Follower2 比“仅软降权”略差，不能宣传为对所有僚机必然更优。

当前隔离规则的准确名称是：**CUSUM 软降权 + 在线报警边选择性隔离**。它确实会阻止报警边
进入新的窗口帧，属于硬隔离，不是“仅软降权”。该决策只依赖在线 `alarm_active` 与 CUSUM 值，
不读取 `fault_edge`、真值、故障时间或故障幅值。

## 额外故障验证

| 故障 | Equal-FGO：F1/F2 | 新方法：F1/F2 |
| --- | ---: | ---: |
| `(2,3)`, `+5 m` | 2.380222 / 2.541414 | 0.791750 / 0.961309 |
| `(1,6)`, `+5 m` | 4.885984 / 5.407859 | 1.405535 / 1.661018 |

两组均使用 `seeds=[21,22,23]`；故障边中位权重均为 `0.05`，平均报警延迟为 `1.0 s`。

## 健康场景与数值稳定性

健康三种子下，新方法全程 RMSE 为 `0.806037 / 0.958696 m`，原始 Equal-FGO 为
`0.822036 / 0.975549 m`。但 seed 21、22 分别出现 2、3 次确认报警；平均约 2.97% 的
健康边历元权重小于 0.95。因此当前版本不能声称“零误报”。

在 seed 23、窗口长度 10 的全程数值诊断中，滑窗线性系统每帧满列秩，最小奇异值约 `0.814`，
最大条件数约 `13.1`，信息矩阵最小倒条件数约 `2.11e-3`；此工况下 `pinv` 没有掩盖秩亏。
这不是所有种子、所有拓扑的普遍证明，后续应持续记录该诊断。

## CUSUM 共识诊断

主故障中目标边的共识可用率为 98.33%，可用时中位邻居数为 3。`leader_only` 模式下，
故障边本身不用于自己的参考；对健康边，三个参考边中即便包含一条故障边，中位数仍对一个
离群值稳健。

## 推荐的后续顺序

1. 论文主结果先报告“仅软降权”和“软降权+隔离”两组，明确隔离是独立模块。
2. 扩展健康种子数，量化误报警率，再决定是否调整 CUSUM 参数；本轮不调参。
3. 将运动因子协方差从固定经验值升级为由 SINS/KF 传播协方差推导，并处理与反馈的相关性。
4. 如需严格固定滞后平滑，增加边缘化先验；在此之前保持当前算法命名。
