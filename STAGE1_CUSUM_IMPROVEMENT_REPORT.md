# Stage 1 CUSUM 持续弱故障改进报告

## 结论

`3 m / 50 s` 持续阶跃在理论上可以由带符号 CUSUM 检测和软降权抑制；它不是“理论上不可处理”的故障。但在本工程中，SINS/KF 预测误差会在各边间传播，弱故障与健康预测失配同量级，因此健康保护、弱故障灵敏度和恢复速度存在可量化的折中。当前实现提升了 `3 m` 的故障窗口表现，并保留强故障抑制；独立验证显示健康指标仍未达到目标，不能宣称全部验收通过。

## 根因

1. 原权重映射从任何非零 CUSUM 即开始降权，健康边也会被软降权。
2. 原基线冻结依赖单步变化；持续同号弱偏差可能与健康预测缓慢漂移混淆。
3. CUSUM 用同一遗忘因子累计和恢复，故障清除后恢复慢。
4. 仅依赖全局暖机时间不能正确处理晚出现或失联重连的边。

## 实施的改进

- 健康死区：`c <= 1.25` 时权重严格为 `1`；超过后维持连续软降权，最低仍为 `0.05`。
- 独立报警状态：每边维护 `alarm_active`、`confirm_count`、`release_count`；报警与权重阈值事件分别报告。
- 严格基线状态：基线更新受 CUSUM、创新、创新变化和报警状态共同约束；记录冻结、解冻、更新次数。
- 独立边标定：新增 `calibrating`、`baseline_valid`、`baseline_unverified`；可信基线跨失联保留，长失联只重置 CUSUM/报警。
- 快速恢复：健康创新连续出现时采用 `cusum_release_lambda=0.45`，与故障累计 `lambda=0.90` 分离。
- 诊断：逐边创新统计、连续符号长度、权重分布、报警、基线事件、确认报警延迟、权重下降延迟、报警解除和权重恢复延迟。
- 添加 `run_stage1_cusum_validation.m`，固定参数批量验证正负阶跃和另一条有效边。

在线函数 `compute_signed_cusum_edge_weights` 未接受或读取真值、`fault_enable`、故障边、故障时间或故障幅值。测距因子仍仅用一次 `sqrt(weight)` 加权；没有删除任何有效边。

## 修改文件

- `stage1_cusum_default_config.m`：死区、报警、释放、逐边标定和基线配置。
- `compute_signed_cusum_edge_weights.m`：每边状态机、连续权重、独立标定、冻结/解冻、快速释放和诊断字段。
- `run_stage1_cusum_comparison.m`：故障窗口参数校验、报警/逐边诊断输出。
- `test_stage1_cusum_components.m`：新增独立标定、正负故障、单调降权、失联保留基线、初始极端故障保护和恢复测试。
- `run_stage1_cusum_validation.m`：固定参数的正负阶跃/异边批量验证入口。
- `使用说明_第一阶段_CUSUM.md`：更新运行与诊断说明。

## 实际执行的测试

```matlab
test_stage1_cusum_components
test_stage1_cusum_smoke
```

均通过；其中 smoke test 还通过了确定性 Equal 重复性和原始 Equal 回归检查。

训练种子为 `1:5`，仅用于比较参数机制；独立验证种子为 `6:20`，未再用于调参。

## 已执行结果

### 训练种子 `1:5`

| 故障 | 全时段 F1 / F2 相对 Equal | 故障窗口 F1 / F2 相对 Equal | 状态 |
| --- | ---: | ---: | --- |
| `+3 m, 100-150 s` | `-2.79% / -10.04%` | `-9.47% / -20.22%` | 两架窗口均改善 |
| `+5 m, 100-150 s` | `-10.49% / -19.35%` | `-16.07% / -29.88%` | 两架窗口均改善 |
| `+8 m, 100-110 s` | `-34.03% / -43.99%` | `-58.00% / -65.60%` | 保留强故障能力 |

同一训练运行的健康场景为 `+4.88% / +4.84%`，未满足健康 RMSE `<=2%` 的验收目标。`+5 m` 与 `+8 m` 的部分种子权重恢复时间仍超过 `15 s`，未满足全部恢复验收目标。

### 独立验证种子 `6:20`：`+3 m, 100-150 s`

完整 15 个种子按五个三种子批次实际运行并合并均值：

| 场景 | Follower 1 | Follower 2 |
| --- | ---: | ---: |
| 健康全时段 CUSUM 相对 Equal | `+15.36%` | `+17.87%` |
| 故障全时段 CUSUM 相对 Equal | `-2.64%` | `-11.01%` |
| 故障窗口 CUSUM 相对 Equal | `-10.71%` | `-23.87%` |

因此，`+3 m` 故障窗口“两个 follower 均不劣于 Equal、平均改善至少 5%”通过；健康 RMSE、健康低权重比例和零误报目标不通过。尚未运行完整 `6:20` 的其他五类故障，不能声称那些独立验证已完成；可通过 `run_stage1_cusum_validation(6:20)` 继续执行。

## 可辨识性与剩余风险

若某条边首次出现时已带有弱恒定偏置，且没有历史基线、真值或额外独立参考，该边自身无法区分正常初始偏置和故障。代码会将这种基线标记为 `baseline_unverified`；极端初始样本会被拒绝标定。要进一步满足严格健康指标，需要在下一阶段引入经过验证的独立预测不确定性建模或网络一致性模型；本次测试的简单邻边中位数一致性在训练种子上不稳定，因此默认关闭，未作为结论依据。
